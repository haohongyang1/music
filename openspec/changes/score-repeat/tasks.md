## 1. 数据结构扩展

- [ ] 1.1 在 `scoreData.js` 中扩展乐谱数据，添加 `sections` 和 `repeats` 字段
- [ ] 1.2 为「红色高跟鞋」手动添加段落和反复标注示例（MVP）
- [ ] 1.3 编写 `getSectionAtPosition()` 辅助函数，根据滚动位置获取当前段落
- [ ] 1.4 编写 `getNextSection()` 辅助函数，获取下一个段落
- [ ] 1.5 编写 `validateScoreData()` 数据校验函数
- [ ] 1.6 添加 `STORAGE_KEYS` 常量，统一管理 localStorage 键名

## 2. 反复状态机实现

- [ ] 2.1 在 `PlaybackView` 中添加状态：`currentSectionId`、`repeatCount`、`skipRepeat`、`showSectionIndicator`
- [ ] 2.2 从 localStorage 读取 `skipRepeat` 和 `showSectionIndicator` 状态，变化时持久化
- [ ] 2.3 实现段落切换逻辑，根据滚动位置自动识别进入新段落
- [ ] 2.4 实现 `isInTriggerZone()` 函数，判断是否在反复触发区域（含 `maxScroll` 容错）
- [ ] 2.5 实现反复跳转逻辑，到达反复结束点时判断是否需要回跳（含目标段落不存在的处理）
- [ ] 2.6 实现「跳过反复」功能，标记后直接播放完整版

## 3. UI 更新

- [ ] 3.1 添加段落状态指示器，显示当前段落和重复次数
- [ ] 3.2 添加「跳过反复」Toggle 开关按钮
- [ ] 3.3 添加「眼睛」图标，切换段落指示器自动/手动显示模式
- [ ] 3.4 实现段落切换时的淡入淡出动画（各 150ms）
- [ ] 3.5 实现段落指示器自动隐藏逻辑（2.5 秒）和常驻显示切换
- [ ] 3.6 适配移动端样式，确保控制栏不遮挡乐谱
- [ ] 3.7 处理边界场景 UI（段落不存在、数据无效等）

## 4. 验证和测试

- [ ] 4.1 在本地测试红色高跟鞋的小反复播放
- [ ] 4.2 测试跳过反复功能及状态持久化
- [ ] 4.3 测试段落指示器自动/手动显示切换
- [ ] 4.4 测试未标注乐谱的向后兼容性
- [ ] 4.5 测试边界场景（快速滚动、小段落、maxScroll=0 等）
- [ ] 4.6 运行 lint 和 build，检查是否有回归
- [ ] 4.7 在移动端/平板测试触摸交互

---

## MVP 范围

本迭代实现：
- 单段落小反复（A 段重复）
- 一首示例乐谱（红色高跟鞋），手动标注段落数据
- 基础段落指示器

## 后续扩展

### 标注工具（独立功能块）
详见 `specs/annotation-tool/`，后续迭代实现：

- [ ] T1 在播放控制栏添加「📝 标注」按钮（仅开发环境）
- [ ] T2 实现标注模式状态切换
- [ ] T3 实现标注面板 UI（段落列表、操作按钮）
- [ ] T4 实现段落添加功能（在当前滚动位置）
- [ ] T5 实现段落边界拖拽调整
- [ ] T6 实现反复规则添加功能
- [ ] T7 实现 JSON 导出功能
- [ ] T8 实现草稿自动保存和恢复（localStorage）
- [ ] T9 实现预览播放功能
- [ ] T10 实现边界重叠检测和处理

### 其他扩展
- 多段落各有反复规则
- 跨段落反复
- 多段反复（1st/2nd endings）
- 跳跃反复（D.C./D.S.）