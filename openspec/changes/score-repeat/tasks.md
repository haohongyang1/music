## 1. 数据结构扩展

- [ ] 1.1 在 `scoreData.js` 中扩展乐谱数据，添加 `sections` 和 `repeats` 字段
- [ ] 1.2 为现有乐谱（红色高跟鞋）添加段落和反复标注示例
- [ ] 1.3 编写 `getSectionAtPosition()` 辅助函数，根据滚动位置获取当前段落
- [ ] 1.4 编写 `getNextSection()` 辅助函数，获取下一个段落

## 2. 反复状态机实现

- [ ] 2.1 在 `PlaybackView` 中添加状态：`currentSection`、`repeatCount`、`skipRepeat`
- [ ] 2.2 实现段落切换逻辑，根据滚动位置自动识别进入新段落
- [ ] 2.3 实现反复跳转逻辑，到达反复结束点时判断是否需要回跳
- [ ] 2.4 实现「跳过反复」功能，标记后直接播放完整版

## 3. UI 更新

- [ ] 3.1 添加段落状态指示器，显示当前段落和重复次数
- [ ] 3.2 添加「跳过反复」开关按钮
- [ ] 3.3 适配移动端样式，确保控制栏不遮挡乐谱
- [ ] 3.4 添加段落切换的视觉反馈（如淡入淡出）

## 4. 验证和测试

- [ ] 4.1 在本地测试红色高跟鞋的小反复播放
- [ ] 4.2 测试跳过反复功能
- [ ] 4.3 测试未标注乐谱的向后兼容性
- [ ] 4.4 运行 lint 和 build，检查是否有回归